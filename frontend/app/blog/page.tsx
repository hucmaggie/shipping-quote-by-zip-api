import { StubPage } from "../../components/stub-page";

export const metadata = { title: "Blog | Pause-Health.ai" };

export default function BlogPage() {
  return (
    <StubPage
      eyebrow="Blog"
      title="Stories from the frontier of menopause care."
      intro="Our team writes about clinical evidence, AI model design, provider workflows, and the lived experience of women in midlife. The first essays will be published soon — subscribe via the footer to get them in your inbox."
    />
  );
}
